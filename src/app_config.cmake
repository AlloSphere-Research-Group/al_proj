message("Reading app.cmake...")

# list(APPEND app_include_dirs ${PATH_TO_INCLUDE})

# list(APPEND app_link_libs ${PATH_TO_LIB})

# list(APPEND app_definitions)

# list(APPEND app_compile_flags)

# list(APPEND app_linker_flags)